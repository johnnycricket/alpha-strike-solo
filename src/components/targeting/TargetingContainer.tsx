import { TableInterface } from "../../types/TableInterface";

interface TT {
    targetType: TableInterface[];
    targetTitle: String;
}

const TargetingContainer = (props: TT) => {

}

export default TargetingContainer